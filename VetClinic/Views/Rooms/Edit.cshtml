@model Room

@section title {
    @if (Model != null)
    {
        <h3 style="font-family: 'Times New Roman', Times, serif; ">@VetClinic.Resources.Resource.Room: @Model.RoomNumber</h3>
    }
}

@if (Model != null)
{
    <partial name="_Room" model="@Model" />
}
