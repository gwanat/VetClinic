@model List<Room>

@section title {
    <h2 style="font-family: 'Times New Roman', Times, serif; ">Rooms</h2>
}

@if (Model != null && Model.Count() > 0)
{
    <table class="table table-striped">
        <thead>
            <tr class="text-center">
                <th>Room Number</th>
                <th>Type</th>
                <th>Status</th>
                <th>Description</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var room in Model)
            {
                <tr>
                    <td class="align-middle text-center">@room.RoomNumber</td>
                    <td class="align-middle text-center">@room.Type</td>
                    <td class="align-middle text-center">@(room.IsOccupied ? "Occupied" : "Not occupied")</td>
                    <td class="align-middle text-center">@room.Description</td>
                    <td class="align-middle text-center">
                        <a class="btn btn-outline-primary" asp-controller="rooms" asp-action="edit" asp-route-id="@room.RoomId"><i class="bi bi-pencil-square"></i> Edit</a>
                        <a class="btn btn-outline-danger" asp-controller="rooms" asp-action="delete" asp-route-id="@room.RoomId" onclick="return confirmDelete()"><i class="bi bi-trash"></i> Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
<br />
<a class="btn btn-outline-success" asp-controller="rooms" asp-action="add"><i class="bi bi-plus-circle"></i> Add Room</a>

@section Scripts {
    <script>
        function confirmDelete() {
            return confirm("Are you sure you want to delete this room?");
        }
    </script>
}
