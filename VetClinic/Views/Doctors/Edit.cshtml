@model Doctor

@section title {
    @if (Model != null)
    {
        <h3 style="font-family: 'Times New Roman', Times, serif; ">Doctor: @Model.Name</h3>
    }
}

@if (Model != null)
{
    <partial name="_Doctor" model="@Model" />
}
