@model PatientViewModel

@section title {
    @if (Model != null)
    {
        <h3 style="font-family: 'Times New Roman', Times, serif; ">Patient: @Model.Patient?.PatientName</h3>
    }
}

@if (Model != null)
{
    <partial name="_Patient" model="Model" />
}