@model PatientViewModel

@section title {
    @if (Model != null)
    {
        <h3>Patient: @Model.Patient?.PatientName</h3>
    }
}

@if (Model != null)
{
    <partial name="_Patient" model="Model" />
}