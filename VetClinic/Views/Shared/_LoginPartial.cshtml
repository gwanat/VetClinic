@using Microsoft.AspNetCore.Identity
@using VetClinic.Areas.Identity.Data

@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

<ul class="navbar-nav">
@if (SignInManager.IsSignedIn(User))
{
     <li class="nav-item">
            <a id="manage" class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">@VetClinic.Resources.Resource.Hello @UserManager.GetUserName(User)!</a>
            
    </li>
    <li class="nav-item">
        <form id="logoutForm" class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <button id="logout" type="submit" class="btn btn-outline-danger my-2 my-sm-0">
                    <i class="bi bi-box-arrow-right"></i> @VetClinic.Resources.Resource.Logout
                </button>
        </form>
    </li>
}
else
{
    <li class="nav-item">
        <a class=" btn btn-outline-primary my-2 my-sm-0" id="register" asp-area="Identity" asp-page="/Account/Register">
                <i class="bi bi-person-plus"></i> @VetClinic.Resources.Resource.Register
        </a>
            <span style="padding: 5px;"></span>
    </li>
    <li class="nav-item">
            <a class="btn btn-outline-success my-2 my-sm-0" id="login" asp-area="Identity" asp-page="/Account/Login">
                <i class="bi bi-box-arrow-in-right"></i> @VetClinic.Resources.Resource.Login
        </a>
    </li>
}
</ul>
